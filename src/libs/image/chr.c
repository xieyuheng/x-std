#include "index.h"

uint8_t *
chr_subimage(
    uint8_t *bytes,
    size_t row, size_t col,
    size_t width, size_t height
) {
    (void) row;
    (void) col;
    (void) width;
    (void) height;

    return bytes;
}
